- name: install development dependencies
  apt:
    name: "{{ packages }}"
    state: present
  vars:
    packages:
      - build-essential
      - cmake
      - tree
      - pkg-config
      - gnome-tweak-tool
      - python3-pip
      - clang
      - cppcheck
      - curl
  become: True
