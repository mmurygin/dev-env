---
- name: install vim
  apt:
    name: vim
    state: present
  become: yes

- name: copy vim configs
  copy:
    src: files/.vimrc
    dest: ~/.vimrc

- name: include pathogen plugin manager
  include_tasks: pathogen.yml
