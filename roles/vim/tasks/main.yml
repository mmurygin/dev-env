---
- name: install vim
  apt:
    name: vim
    state: present
  become: yes

- name: install plugins
  include_tasks: plugins.yml

- name: copy vim configs
  copy:
    src: files/.vimrc
    dest: ~/.vimrc
