---
- name: setup desktop environment
  gather_facts: True
  hosts: localhost
  roles:
    # UI
    - chrome
    - gnome-shell

    # common dev
    - configs
    - dev
    - xclip
    - cron-update-packages

    # languages
    - go
    - python3

    # autocomplete (required golang)
    - fzf

    # editors
    - vim
    - tmux

    # virtualization
    - kvm
    # - virtualbox


    # kubernetes
    - kube-client
    # - k9s
    # - helm-client
    # - helmfile

    # devops
    - vagrant
    - terraform
    - vault
    - packer
    # - chef

    # clouds
    - aws
